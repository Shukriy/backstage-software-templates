apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{values.component_id}}
  description: Python app that displays the time with the name ${{values.component_id}}
  annotations:
    github.com/project-slug: Shukriy/${{values.component_id}}
    backstage.io/techdocs-ref: dir:.
spec:
  type: service
  owner: development
  lifecycle: experimental
